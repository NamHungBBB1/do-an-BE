package com.doan.game.auth.domain;

/**
 * Hai phạm vi dữ liệu TÁCH HẲN (chốt: "riêng luôn — không liên quan").
 *
 * Nhà không thấy lớp, lớp không thấy nhà. Ranh giới ĐỐI XỨNG.
 * Thứ duy nhất đi qua: LEVEL và DANH HIỆU — con số tổng hợp, không phải lịch sử từng lượt.
 */
public enum LearningContext { FAMILY, CLASS }
